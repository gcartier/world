;;;===============
;;;  WorldScheme
;;;===============
;;;
;;;; World Context
;;;
;;;  The Initial Developer of the Original Code is Guillaume Cartier.
;;;  Portions created by the Initial Developer are Copyright (C) 2012-2018
;;;  the Initial Developer. All Rights Reserved.
;;;
;;;  Contributor(s):


(module world.context jazz


(import (world)
        (world.autoload))


;;;
;;;; Window
;;;


(definition protected *current-window*
  #f)


(definition public (set-current-window window)
  (set! *current-window* window)
  (set-?w window))


;;;
;;;; Interface
;;;


(definition protected *current-interface*
  #f)


(definition public (set-current-interface interface)
  (set! *current-interface* interface)
  (set-?i interface))


;;;
;;;; Tier
;;;


(definition protected *current-tier*
  #f)


(definition public (set-current-tier tier)
  (set! *current-tier* tier)
  (set-?t tier))


;;;
;;;; Processor
;;;


(definition protected *current-processor*
  #f)


(definition public (set-current-processor processor)
  (set! *current-processor* processor)
  (set-?p processor))


;;;
;;;; World
;;;


(definition protected *current-world*
  #f)


(definition public (set-current-world world)
  (set! *current-world* world)
  (set-? world))


;;;
;;;; Universe
;;;


(definition protected *current-universe*
  #f)


(definition public (set-current-universe universe)
  (set! *current-universe* universe)
  (set-?u universe))


;;;
;;;; Zone
;;;


(definition protected *current-zone*
  #f)


(definition public (set-current-zone zone)
  (set! *current-zone* zone)
  (set-?z zone))


;;;
;;;; Me
;;;


(definition protected *current-me*
  #f)


(definition public (set-current-me me)
  (set! *current-me* me)
  (set-?m me))


;;;
;;;; Camera
;;;


(definition protected *current-camera*
  #f)


(definition public (set-current-camera camera)
  (set! *current-camera* camera)
  (set-?c camera))


;;;
;;;; Motion
;;;


(definition protected *current-motion*
  #f)


(definition public (set-current-motion motion)
  (set! *current-motion* motion)
  (set-?v motion))


;;;
;;;; Music
;;;


(definition protected *current-music*
  #f)


(definition public (set-current-music music)
  (set! *current-music* music)
  (set-?o music))


;;;
;;;; Ambience
;;;


(definition protected *current-ambience*
  #f)


(definition public (set-current-ambience ambience)
  (set! *current-ambience* ambience))


;;;
;;;; Audio
;;;


(definition protected *current-audio*
  #f)


(definition public (set-current-audio audio)
  (set! *current-audio* audio))


;;;
;;;; Video
;;;


(definition protected *current-video*
  #f)


(definition public (set-current-video video)
  (set! *current-video* video))


;;;
;;;; History
;;;


(definition protected *current-history*
  #f)


(definition public (set-current-history history)
  (set! *current-history* history)
  (set-?h history))


;;;
;;;; Tutorial
;;;


(definition protected *current-tutorial*
  #f)


(definition public (set-current-tutorial tutorial)
  (set! *current-tutorial* tutorial))


;;;
;;;; Context
;;;


(definition public inline (current-window) <World-Window>
  *current-window*)


(definition public inline (current-window+) <World-Window+>
  *current-window*)


(definition public inline (current-interface) <World-Interface>
  *current-interface*)


(definition public inline (current-interface+) <World-Interface+>
  *current-interface*)


(definition public inline (current-tier) <Tier>
  *current-tier*)


(definition public inline (current-tier+) <Tier+>
  *current-tier*)


(definition public inline (standalone-tier) <Standalone-Tier>
  *current-tier*)


(definition public inline (client-tier) <Client-Tier>
  *current-tier*)


(definition public inline (server-tier) <Server-Tier>
  *current-tier*)


(definition public inline (processor-tier) <Processor-Tier>
  *current-tier*)


(definition public inline (current-processor) <Processor>
  *current-processor*)


(definition public inline (current-processor+) <Processor+>
  *current-processor*)


(definition public inline (current-world) <World>
  *current-world*)


(definition public inline (current-world+) <World+>
  *current-world*)


(definition public inline (current-universe) <Universe>
  *current-universe*)


(definition public inline (current-zone) <Zone>
  *current-zone*)


(definition public inline (current-zone+) <Zone+>
  *current-zone*)


(definition public inline (current-me) <Player>
  *current-me*)


(definition public inline (current-me+) <Player+>
  *current-me*)


(definition public inline (current-camera) <Camera>
  *current-camera*)


(definition public inline (current-motion) <Motion>
  *current-motion*)


(definition public inline (current-music) <World-Music>
  *current-music*)


(definition public inline (current-ambience) <World-Ambience>
  *current-ambience*)


(definition public inline (current-audio) <World-Audio>
  *current-audio*)


(definition public inline (current-video) <World-Video>
  *current-video*)


(definition public inline (current-history) <History>
  *current-history*)


(definition public inline (current-tutorial)
  *current-tutorial*))
