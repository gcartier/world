;;;==============
;;;  JazzScheme
;;;==============
;;;
;;;; World Debuggee
;;;


(module world.debuggee jazz


(import (jazz.jrm)
        (world.debuggee.implementation)
        (world.debuggee.stub))


(definition public (make-world-debuggee)
  (new World-Debuggee-Local-Proxy (new World-Debuggee)))


(register-object~ (get-local-register) 'world-debuggee (make-world-debuggee)))
