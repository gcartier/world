;;;==============
;;;  JazzScheme
;;;==============
;;;
;;;; Element Faces
;;;


(module world.face jazz


(import (jazz.geometry)
        (jazz.graphic)
        (jazz.graphic.opengl.glew)
        (jazz.io)
        (jazz.library)
        (jazz.literals)
        (jazz.media)
        (jazz.network)
        (jazz.platform)
        (jazz.platform.cairo)
        (jazz.runtime)
        (jazz.system)
        (jazz.ui)
        (jazz.ui.dialog)
        (jazz.ui.window)
        (world)
        (world.foreign)
        (world.geometry)
        (world.polygon)
        (world.texture)
        (time))


(definition public (make-face texture texture-coordinates texture-depth polygon)
  (new Face texture texture-coordinates texture-depth polygon))


;;;
;;;; Face
;;;


(class Face extends Object
  
  
  (slot texture                       getter generate)
  (slot texture-coordinates           getter generate)
  (slot texture-depth                 getter generate)
  (slot polygon             <Polygon> getter generate)
  (slot indice              <fx+>     initialize #f accessors generate)
  
  
  (method override (initialize texture texture-coordinates texture-depth polygon)
    (nextmethod)
    (set! texture~self texture)
    (set! texture-coordinates~self texture-coordinates)
    (set! texture-depth~self texture-depth)
    (set! polygon~self polygon))
  
  
  (method public (triangle-count)
    (triangle-count~ polygon))
  
  
  (method public (add-triangles add)
    (add-triangles~ polygon texture-coordinates texture-depth add))))
