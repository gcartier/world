(package world.website
  
  (root "src")
  (products (world.website (update world.website)
                           (dependencies jazz)))
  (profiles (Website (unit profile.website)))
  (project ".project"))
