(package %profile-unit%
  
  (products (%profile-unit% (update %profile-unit%.%profile%)
                            (dependencies jazz)))
  (profiles (%profile% (appl world) (unit %profile-unit%)))
  (project ".project"))
